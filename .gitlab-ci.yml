include:
  - project: $CICD_INDUS_PROJECT
    ref: $CICD_INDUS_BRANCH
    file:
      - pipeline/go.yml
      - pipeline/pages.yml

pages:
  image: registry.gitlab.com/pages/hugo/hugo_extended:latest
  variables:
    DIST_FOLDER: public
  before_script:
    - apk add make go
    - make build
